name: Build AntherChat APK

on:
  push:
    branches:
      - main
      - master
      - sc
  pull_request:
    branches:
      - main
      - master
      - sc
  workflow_dispatch: # Allows manual trigger from GitHub Actions web tab
  release:
    types: [published]

jobs:
  build:
    name: Build AntherChat Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Make Gradle Executable
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew :vector-app:assembleDebug --no-daemon

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AntherChat-Debug-APK
          path: vector-app/build/outputs/apk/**/debug/*.apk
          retention-days: 7

  release:
    name: Attach APK to GitHub Release
    needs: build
    if: github.event_name == 'release' && github.event.action == 'published'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Make Gradle Executable
        run: chmod +x gradlew

      - name: Build Release APK
        run: ./gradlew :vector-app:assembleDebug --no-daemon

      - name: Upload APK to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: vector-app/build/outputs/apk/**/debug/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

